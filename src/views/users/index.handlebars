
    <h1>Users</h1>
    <a href="/users/create" class="create-btn">Create A New User</a>
    <div class="user-list">
        {{#each users}}
            {{> userCard this}}
        {{else}}
            <p>Foydalanuvchilar mavjud emas.</p>
        {{/each}}
    </div>
